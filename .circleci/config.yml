version: 2.1

jobs:
  build:
    machine:
      image: ubuntu-2404:current
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: "echo Hello, World!"
      - run:
          name: Install tools & libraries
          command: sudo apt update && sudo apt install wget git build-essential cmake ninja-build libssl-dev qtbase5-dev qttools5-dev qttools5-dev-tools pkg-config zlib1g-dev -y
      - run:
          name: Install Boost
          command: |
            wget https://archives.boost.io/release/1.88.0/source/boost_1_88_0.tar.gz
            tar xf boost_1_88_0.tar.gz
            cd boost_1_88_0
            ./bootstrap.sh
            ./b2 stage --stagedir=./ --with-headers
      - run:
          name: Install libtorrent
          command: |
            git clone --depth 1 --recurse-submodules https://github.com/arvidn/libtorrent.git
            cd libtorrent
            cmake -B build -G "Ninja" -DBUILD_SHARED_LIBS=OFF -DCMAKE_BUILD_TYPE=RelWithDebInfo -DCMAKE_CXX_STANDARD=20 -DCMAKE_EXPORT_COMPILE_COMMANDS=ON -DBOOST_ROOT="../boost_1_88_0" -Ddeprecated-functions=OFF
            cmake --build build --verbose
            sudo cmake --install build
      - run:
          name: Build qBittorrent
          command: |
            git clone https://github.com/qbittorrent/qBittorrent
            cd qBittorrent
            CXXFLAGS="$CXXFLAGS -DQT_FORCE_ASSERTS -Werror" \
            LDFLAGS="$LDFLAGS -gz" \
            cmake -B build -G "Ninja" -DCMAKE_BUILD_TYPE=RelWithDebInfo -DCMAKE_EXPORT_COMPILE_COMMANDS=ON -DBOOST_ROOT="../boost_1_88_0" -DTESTING=ON -DVERBOSE_CONFIGURE=ON -DGUI=OFF
            cmake --build build
            cmake --build build --target check
            DESTDIR="qbittorrent" cmake --install build

workflows:
  say-hello-workflow:
    jobs:
      - build
